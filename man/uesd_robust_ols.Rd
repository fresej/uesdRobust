% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uesd_robust_ols.R
\name{uesd_robust_ols}
\alias{uesd_robust_ols}
\title{OLS UESD with robust or cluster‐robust SE and optional placebo inference}
\usage{
uesd_robust_ols(
  df,
  outcome,
  date_num,
  bw = 15,
  cutoff = 0,
  cluster = NULL,
  controls = NULL,
  placebos = FALSE,
  excluded = 3
)
}
\arguments{
\item{df}{A data.frame containing your data.}

\item{outcome}{String name of the numeric outcome variable.}

\item{date_num}{String name of the numeric running date variable.}

\item{bw}{Numeric half‐width of the window around \code{cutoff}.}

\item{cutoff}{Numeric cutoff on \code{date_num} defining the treated (≥cutoff) vs control (<cutoff) groups. Defaults to 0.}

\item{cluster}{NULL (default) for heteroskedasticity‐robust SE, or a string naming a column in \code{df} on which to cluster.}

\item{controls}{NULL (default) or a character vector of other column-names in \code{df} to add as linear controls.}

\item{placebos}{Logical; if TRUE, runs regressions at every feasible cutoff (skipping \code{excluded} after the main), and computes a placebo‐robust p‐value and CI.}

\item{excluded}{Integer; number of subsequent cutoffs after the main one to skip in the placebo loop (default = 3).}
}
\value{
Invisibly returns a list with elements:
\itemize{
\item \code{estimate}, \code{se}, \code{t_value}, \code{ci} (length‐2 numeric), \code{p_value}, \code{n_obs}
\item if \code{placebos = TRUE}: also \code{placebo_t_values}, \code{placebo_p_value}, \code{ci_placebo}
}
}
\description{
OLS UESD with robust or cluster‐robust SE and optional placebo inference
}
