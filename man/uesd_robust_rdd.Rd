% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uesd_robust_rdd.R
\name{uesd_robust_rdd}
\alias{uesd_robust_rdd}
\title{RDD UESD with conventional, bias-corrected, or robust inference and optional placebo tests}
\usage{
uesd_robust_rdd(
  df,
  outcome,
  running,
  cutoff = 0,
  bw = NULL,
  placebos = FALSE,
  excluded = 3,
  se_type = c("conventional", "bias-corrected", "robust")
)
}
\arguments{
\item{df}{A data.frame containing your data.}

\item{outcome}{String name of the numeric outcome variable.}

\item{running}{String name of the numeric running (forcing) variable.}

\item{cutoff}{Numeric discontinuity point on \code{running}. Defaults to 0.}

\item{bw}{Numeric half-width of the RDD window (h). If NULL (default), uses rdrobust’s optimal bandwidth.}

\item{placebos}{Logical; if TRUE, runs placebo RDDs at every feasible cutoff (skipping \code{excluded} after the main) and computes a placebo-robust p-value and CI.}

\item{excluded}{Integer; number of subsequent cutoffs after the main one to skip in the placebo loop (default = 3).}

\item{se_type}{Character; which rdrobust row to use for placebo inference. One of
"conventional" (row 1), "bias-corrected" (row 2), or "robust" (row 3).  Default: "conventional".}
}
\value{
Invisibly returns a list with:
\itemize{
\item h_used, n_obs, se_type
\item \strong{All three} rdrobust estimates in coef_all, se_all, z_all, p_all, ci_all  (named by row)
- \strong{Selected} row statistics in estimate, se, z_value, p_value, ci
\item if placebos = TRUE, also z_values, placebo_p_value, ci_placebo
}
}
\description{
RDD UESD with conventional, bias-corrected, or robust inference and optional placebo tests
}
